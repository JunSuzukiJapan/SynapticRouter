# SynapticRouter

Minimal Synaptic Routing Architecture experiments for `copy`, `reverse`, `paren`, and `addmod` tasks.

Run a single task:

```bash
python src/sra_experiment.py --task reverse --steps 2000
```

Run the default task suite sequentially:

```bash
python src/sra_experiment.py --task-suite
```
