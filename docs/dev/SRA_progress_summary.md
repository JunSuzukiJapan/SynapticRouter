# SRA Progress Summary

## 日付
2026-05-03

## 現在の進捗
- `src/sra_experiment.py` を設計に沿う形で改良。
- `TinySynapse` に自己注意を導入し、入力系列の文脈をより適切に処理。
- `Router` と `Synapse Space` の設計を強化し、ルーティングが安定するように調整。
- ソース／ターゲットの区別を `segment embedding` で明示化。
- ターゲット系列に対して相対位置埋め込みを追加し、EOS 予測の安定化を図った。
- 自己生成プレフィックス学習を追加し、推論時の露出バイアスを低減。
- `copy` タスクに対して学習検証を実施し、ランダム100例の推論でエラー0件を達成。

## 主要な成果
- `copy` タスクにおいて、最大系列長3までの生成が安定。
- 長さ2の系列終端で発生していた `EOS` 誤予測問題を改善するために、ターゲット位置情報とセグメント情報を追加。
- 実装変更をコミットし、GitHub へプッシュ済み。
- マイルストーンタグ `v0.1.2` を作成・プッシュ。

## 追加で残した変更
- `src/sra_experiment.py` の大幅な改修。
- `docs/dev` に進捗ログファイルとして以下を残し、今後の追跡に備えた。
  - `docs/dev/SynapticRouter_full_log.md`
  - `docs/dev/SynapticRouter_verbatim_like_log.md`

## 次回にすること
1. `reverse` タスクを同じ仕組みで検証し、モデルが順序反転を学習できるか確認する。
2. `paren`（括弧バランス判定）や `addmod` など、設計書で想定される他のタスクへの適用を試す。
3. 系列長をさらに伸ばして（`max_len=5` 以上）、一般化性能を評価する。
4. `Router` の負荷分散や `synapse` の専門化挙動を可視化・安定化するための追加ログを整備する。
5. 可能であれば、 `docs/dev/設計.md` の設計意図に沿った実験結果を別途整理し、簡潔な対応表を作成する。

## メモ
- `docs/dev/設計.md` は今回変更しない。
- 現在の改善は `copy` タスクにフォーカスしており、他タスクへの拡張は次回の優先事項。
